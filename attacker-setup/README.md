# Attacker Server Setup

This directory contains a script to automatically configure a hardened SSH server for receiving reverse tunnels.

## Prerequisites

- A Linux server with a public IP address
- Root or sudo access
- OpenSSH server installed

## Quick Usage

```bash
sudo ./setup-attacker.sh